[project]
name = "my_lbd_app"
version = "0.1.1"
requires-python = "==3.12"
dependencies = [
    "boto3",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
