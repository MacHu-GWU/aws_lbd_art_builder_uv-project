[project]
name = "my_lbd_app"
version = "0.1.1"
requires-python = ">=3.11"
dependencies = [
    "attrs",
    "boto3",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
